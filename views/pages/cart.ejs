<!--panier - user page-->
<!--fix buttons-->
<!DOCTYPE html>
<html lang="fr">
<%- include('../partials/head') %>

<body>
<%- include('../partials/user_navbar') %>

<div class="center">
    <div class="title">Votre panier contient les éléments ci-dessous:</div>
    <!-- Liste des commandes-->
    <table class=tableau>
        <tr>
            <!-- <th>Catégorie du produit</th> -->
            <th>Produit</th>
            <th>Quantité</th>
            <th>Date sortie</th>
            <th>Date retour</th>

        </tr>
        <% if (cart !== undefined ) { %>
            <% for(element in cart) { %>
                <tr>
                    <td><%= cart[element]["name"] %></td>
                    <td><%= cart[element]["quantity"] %></td>
                    <td><%= cart[element]["start_date"] %></td>
                    <td><%= cart[element]["end_date"] %></td>
                    <td>
                        <button>🗑️</button>
                    </td>
                </tr>
        <% } } %>

    </table>
    <button class="log"><a class="link" href='/checkout' class="log">Confirmer la commande</a></button>
</div>

</body>
</html>   